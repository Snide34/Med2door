# Med2door
Med2Door is a full-stack health-tech mobile app addressing **large dataset performance** issues. Built with **Flutter** and **Supabase (PostgreSQL)**, it manages 1,085 medicines using **lazy loading** and **image caching** for smooth UI. It also enforces medical compliance via prescription-based access logic.
